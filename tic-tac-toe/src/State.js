const playing = Symbol('playing');
const player1Won = Symbol('player1Won');
const player2Won = Symbol('player2Won');
const draw = Symbol('draw');

export default {playing, player1Won, player2Won, draw};